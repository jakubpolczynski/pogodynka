{
    "city": {{city }},
    "country": {{country }},
    "measurements": [
        {% for measurement in measurements %}
        {
            "date": "{{ measurement.date | date('Y-m-d') }}",
            "celsius": "{{ measurement.celsius  * 9 / 5 + 32}}",
        }
        {% if not loop.last %},{% endif %}
        {% endfor %}
    ]
}